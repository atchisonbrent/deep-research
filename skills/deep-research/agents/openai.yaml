interface:
  display_name: Deep Research
  short_description: Auditable research with calibrated evidence
  default_prompt: Use $deep-research to investigate this question, preserve uncertainty, and produce a validated report when the work is substantial.
policy:
  allow_implicit_invocation: true
